window.addEventListener('load', function() {
  languagesContents();
  setInterval(displayTime, 1);
  iconAllLoad();
  autoLanguageLoadNew();
  initializeGame();
  deviceTypeElementsRegistration();
});